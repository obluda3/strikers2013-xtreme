#ifndef BUTTONS
#define BUTTONS

#include "types.h"

bool IsButtonPushed_OK(int unk);
bool IsButtonPushed_OK_Match();
bool IsButtonPushed_Cancel(int unk);
bool IsButtonPushed_Setting(int unk);
bool IsButtonPushed_Status(int unk);
bool IsButtonPushed_Ready(int unk);
bool IsButtonPushed_MTrance(int unk);

#endif